English · [中文](VALIDATION.zh.md)

# Verification record

Executed on 2026-09-09 and 2026-09-10 with Node 22.22.1 and Apple Swift 6.3.3.

| Surface | Evidence |
| --- | --- |
| TypeScript | `npm run check` passes: strict TypeScript, 66 tests across sixteen files, and all production bundles |
| Core | Durable SQLite metadata/event transactions, request conflict/deduplication, interrupted receipts, approval races, cancel, invalid requests and restart recovery |
| DSH contract | Full Connection HTTP envelopes, exact named arguments, cookie auth, mux events, streamed/final message mapping, approval cancellation race and fail-closed missing credentials |
| Local + Relay | Real loopback servers exercise local token/origin/Host fences, event replay, encrypted cross-client commands, host reconnect and revoked credentials |
| Cryptography | Integrity, peer/direction binding, replay, expiry and pairing round trip |
| Native | Sixteen Swift tests pass: presentation, protocol, QR decoding, live Demo commands/approval/history, live Relay settings, and deployment wire/live API checks |
| PWA | Headless Chrome verifies connect/create/prompt/approve/reload, rename/archive/restore, sidebar interaction and 1440px, 390px, 375px layouts; screenshot inspection confirms the mobile approval controls fit |
| Desktop presentation | Own-window captures of isolated, explicitly labeled fixtures verify workspace sidebar, execution view, Markdown/code/table result with inspector, and approval controls at 1000pt width. Captures use the same SwiftUI content in an AppKit preview host; native toolbar layout is also rendered. Code block alignment was corrected after inspection |
| Shared session management | Local and encrypted clients verify rename/archive/restore and history retention, busy/archive guards; CLI operates on a browser-created session and exports private (0600) Markdown; native live tests read back the same metadata commands |
| Build | Monorepo package/CLI/daemon/Relay bundles and Vite production build; native release `Turnwire.app` with ad-hoc signature |
| Official DSH | Temporarily installed `@deepseek-ai/dsh@0.1.5-alpha.1`; real Host auth, empty-session create/list/resume/follow succeeded with isolated `DSH_HOME` and telemetry disabled |
| DeepSeek credential configuration | Official DSH `--dump-config` confirms both model and web search reference `TURNWIRE_HARNESS_DEEPSEEK_API_KEY`; no key value is stored in the overlay. The live Host was launched from the user's interactive environment; automated verification did not request real model inference |
| Public Remote transport | A temporary Cloudflare HTTPS/WSS tunnel serves only public PWA assets and the encrypted Relay. A Remote client authenticated, read the existing DSH-backed session and retrieved its event history through the public endpoint |
| Public asset boundary | Integration coverage includes the PWA entry point, HEAD, private dotfiles, encoded traversal, symlink escapes and daemon endpoints |
| Phone pairing QR | Generated locally with CoreImage and verified by decoding it with Vision; the pairing remains revocable in the native app |
| Public mobile browser | Chrome at 390×844 loads the existing session through the public encrypted Relay, enables the composer and reconnects after refresh with no overflow or JavaScript errors. Scanned pairings now persist in sessionStorage after their URL fragment is removed |
| Remote mode management | Real local and encrypted Relay connections exercise temporary → self-hosted → off, preserving sessions, same-server key reuse, local-only administration, validation before disconnect, cancellation, persisted off, crash cleanup and retry |
| Client parity | CLI subprocesses, the interactive command dispatcher and typed SDK operate on the same isolated daemon: session creation, encrypted phone snapshot, quoted prompt, remote disable, device revoke, Relay configuration/key reuse, notices, authentication errors and JSON output behavior |
| Interactive terminal | A real PTY verifies `turnwire tui`, shared `ls`, the Relay settings menu, non-echoed secret input, `attach`, Ctrl+C returning to the menu, and clean exit |
| Terminal QR | The real CLI writes a PNG with mode 0600; native Vision decodes it and exactly matches the generated pairing URL |
| Tunnel component | A fresh private directory successfully downloaded and SHA-256 verified the official Cloudflare release, launched a real tunnel and closed it. Child-process tests cover secret environment exclusion, abort, and failure after URL announcement but before tunnel registration |
| Managed temporary public access | The local API started a fresh real tunnel, served the PWA over HTTPS, paired a test phone and returned two existing isolated Demo sessions over the public encrypted WSS connection. Turning access off cleaned up the managed tunnel and Relay |
| Live upgrade | The rebuilt native app connects to the upgraded daemon and the DSH-backed session remains available. The earlier external endpoint was retained during the initial upgrade; the latest restart restores the saved temporary mode and creates a new temporary address, requiring a fresh phone pairing link |

The official DSH smoke test revealed that the overview's `{ args }` is nested inside a Connection RPC request envelope. Implementation, fixtures and documentation were corrected before delivery. Another screenshot capture initially caught the responsive sidebar during its resize transition; the browser check now waits for the transition and confirms the actual mobile layout.

Fresh Quick Tunnel DNS records were not immediately available, and the Mac resolver returned `ENOTFOUND` for some newly created names. The successful managed-tunnel public check obtained the new hostname's actual A records through public DNS-over-HTTPS and used them only inside its test process; HTTPS certificate and hostname checks remained enabled. System DNS was not changed. This verifies the public TLS and encrypted application path, but does not establish immediate reachability through every phone or ISP resolver. The previously provisioned public endpoint was preserved throughout.

Not exercised: real model inference, actual shell/file tools, WAN iPhone connectivity, Docker build/deployment, Gatekeeper notarization or background iOS behavior. Production IP TLS was subsequently verified in the fixed-IP deployment recorded below. No claim of other validations is implied by a passing contract fixture. The PWA shell can be cached; offline task execution is intentionally unavailable.

The Mainland China guidance is based on the linked Cloudflare documentation, not a multi-ISP Mainland China connectivity test. A first native live test failed because the test Relay was launched with the wrong port variable; after using its supported `PORT`, all eleven Swift tests passed against the isolated services.

To rerun both native live tests, start an isolated Demo daemon and Relay, set `TURNWIRE_TEST_DAEMON_CONFIG` to the daemon's `client.json`, and set `TURNWIRE_TEST_RELAY_URL` and `TURNWIRE_TEST_RELAY_TOKEN` while invoking `swift test`. Without those settings the relevant live tests skip; ten standalone tests still run. `scripts/ui-check.mjs` uses `TURNWIRE_HOME` and writes to `TURNWIRE_SCREENSHOTS` (default `/tmp/turnwire-screenshots`).

The 2026-09-10 redesign verification used an isolated Demo daemon on port 19998 and Relay on 19999. Native visual fixtures are separate from user sessions and do not execute tools. CLI, browser and Swift contract checks passed without real model inference. Preview captures are in the task visualization directory under `turnwire/desktop`; mobile browser captures are under `turnwire/web`.

After the redesign, the idle live daemon was upgraded to the verified production bundle; its existing DSH-backed session was preserved and the original DSH Host remained online. The rebuilt native app opened a workspace window and established its event connection. Saved temporary remote access returned online with a new public address. Isolated test services and preview processes were then stopped.


## localhost.run, cpolar and connection verification (2026-09-10)

- `npm run check` passes 42 tests in 11 files and all production builds. New tests cover provider selection via CLI/TUI/SDK, cpolar hidden input and saved-token reuse, private config permissions/removal, foreground child shutdown, credential environment exclusion, localhost.run JSON announcements/address changes, and failed/aborted starts.
- Twelve Swift tests pass, including both live tests against isolated Demo/Relay services on 19998/19999. The live native client reads provider metadata and device presence, rejects first-use cpolar without a Token, and switches Relay/off without losing its local session.
- Encrypted health tests prove relay readiness alone cannot mark a phone connected, a silent Mac times out even with the relay socket open, wrong host/nonce replies are rejected, and old relays without disconnect notifications still expire host device presence. Integration checks read an unconfirmed → connected → offline device through the same local API.
- A real anonymous localhost.run process established a public HTTPS origin. Public HTTPS and encrypted WSS snapshot retrieval passed, with one measured Mac round trip of 439 ms. No system DNS override was needed for this run. This measurement is one connection, not a speed guarantee.
- Headless Chrome loaded that public pairing at 390×844 and 375×667, verified the phone connection bar and Mac device acknowledgement, manual recheck, offline state, reconnection and refreshed pairing persistence. No JavaScript errors or horizontal overflow occurred. Own-window native and phone captures are under the task visualization directory `turnwire/remote`.
- Official cpolar 3.3.18 macOS arm64 bytes were SHA-256 verified against its published Homebrew formula. Actual component startup with an intentionally invalid test Token produced the expected authentication failure. This version's documented `inspect-addr=false` unexpectedly binds `:0`; the adapter instead explicitly binds `127.0.0.1:0` and requests HTTPS only. No valid cpolar account Token was supplied, so authenticated cpolar WAN access remains unverified.

The public check used isolated Demo state, not a real model task. Actual iPhone carrier networks, background iOS behavior and comparative Mainland China ISP performance remain unverified.

After these checks, the idle production daemon and native app were updated. The existing DSH-backed session was preserved, DSH remained online, and the new native process established its local event connection with the remote settings sheet open. The daemon reports all three provider options and retains the user's prior Cloudflare selection; its new temporary address requires a fresh pairing link. Isolated test services and preview processes were stopped. The existing local-browser create/prompt/approval/rename/archive/restore regression also passed after the health UI change.


## Phone Markdown rendering (2026-09-10)

The PWA previously inserted assistant text directly into a pre-wrapped div. It now uses a memoized CommonMark/GFM renderer over unchanged event text. Four regression tests cover Markdown structures, streaming/unclosed and nested fences, hard breaks, message-specific footnote anchors, inert HTML and unsafe URL filtering. The existing content security policy is preserved; image references are explicit links.

`npm run check` passes 46 tests in 12 files and production bundles. `node --import tsx scripts/markdown-ui-check.mjs` starts isolated Demo, daemon and encrypted Relay instances and verifies actual assistant output in Chrome: headings, quotes, task lists, table alignment, code copying, container-only horizontal scrolling at 320/375/390/1440px, incomplete streamed code, completed output and refresh/replay. The same session's original completed text is read back unchanged through the local SDK. Screenshots were visually inspected; table sizing was refined after detecting overly narrow columns. No native or daemon restart is needed for this web asset update. Actual iPhone/Safari testing remains unverified.

The running daemon serves the updated Markdown bundle without restart. During delivery, its previously selected localhost.run tunnel was found stopped (`临时通道已停止`); restoring that transport is separate from the static asset update and changes the temporary pairing address.

## Fixed Relay deployment (2026-09-10)

The user-authorized fixed-IP deployment runs Debian 13.5, an isolated official Node 22.23.2 binary, and a bundled Relay/PWA release under systemd. SHA-256 release verification passed. A standalone package smoke check exercised real encrypted health/snapshot exchange without a server-side `node_modules` tree. Model credentials and host state were excluded from the deployment.

The existing Caddy 2.6.2 process retained its PID and the Hermes site configuration; both Hermes listeners retained their PIDs. Turnwire's Relay binds only `127.0.0.1:9899`, runs as its own user, is enabled at boot, and successfully recovered after an explicit service restart. The original Hermes root continues to return the backend's 404, while the existing model API service root returns 200.

Let's Encrypt staging and production IP certificate issuance passed. External curl, Node fetch/WSS and Chrome accepted the production chain without disabling certificate or hostname validation. `default_sni` covers IP clients without SNI. Certbot's renewal dry run succeeded, the certificate copy/validation/Caddy reload hook passed, and the hourly renewal timer is active and enabled.

An isolated Demo host connected through the public service. Encrypted health, snapshot, session creation/rename, bad host token rejection, public asset equality, missing source maps and private-route 404 checks passed. Chrome at 390×844 loaded the public pairing, completed history loading with an enabled composer, and reconnected after refresh without JavaScript errors or page overflow. Measured encrypted round trips were 373 ms and 632 ms in two runs; these are development-machine measurements, not phone carrier or speed guarantees.

The running production Mac switched through the shared local SDK to the fixed Relay without restarting Core, native app or DSH. Its existing session and pending approvals were preserved and DSH remained online. An ephemeral paired client verified the actual Mac's encrypted snapshot and device acknowledgement, then was revoked; the fixed Relay returned online. Actual iPhone/Safari, cellular networks and comparative Mainland China ISP performance remain unverified. Machine-specific settings are stored in private Turnwire state; see `docs/RELAY-INSTALL.md` for portable operations.

The production Mac's initial 3,885-event replay exposed a shared Relay defect that the small Demo check did not: the authenticated host hit the 1,000-message-per-second phone ingress limit and disconnected. A 4,000-event encrypted replay regression reproduced this failure before the fix. The Relay now exempts authenticated host replay from that phone limit while retaining payload/buffer bounds; a separate authenticated phone flood test still receives close code 4429. The full check passes 48 tests in 13 files, and release `20260910.2` contains the fix. No client protocol or native model change was needed.

After the fix was deployed, two ephemeral public pairings completed encrypted verification against the real production Mac (357 ms and 524 ms health round trips). Chrome at 390×844 also read the actual existing session/history, reached the verified connection state and enabled the composer without page overflow or JavaScript errors. No real model prompt was sent. Both verification devices were revoked and the fixed Relay stayed online.

## Portable one-click deployment (2026-09-10)

The server-specific Caddy/systemd/hook templates were replaced by a TypeScript installer that receives runtime configuration. Actual machine addresses, SSH login and key paths were moved into private Turnwire state, including the prior deployment notes. A repository scan found no occurrences of the actual server IP, SSH key name, user home path or previous host-specific template names in distributable source/docs.

`npm run check` passes 57 tests in 14 files. Nine deployment tests cover required runtime input, command/URL/path injection rejection, configurable IP/domain certificate templates, idempotent Caddy merging with preserved global options/sites, child environment credential exclusion, literal shell quoting, backup restoration with permissions, complete release manifests, macOS metadata-free archives, local authorization, shared asynchronous jobs and CLI/TUI/form parity. A known in-progress job is marked interrupted after daemon restart.

`node --import tsx scripts/native-live-check.mjs` passes all 14 Swift tests, including three live tests against an isolated daemon. The deployment fixture exercises the real native HTTP request/status contract but does not open SSH. The native deployment form was also rendered in its own preview window and visually checked; a window capture was used because the offscreen cache image did not correctly include native control layers.

The portable command then performed a real update using the existing server's private configuration, verified HTTPS and registered an isolated host. A subsequent update through the production daemon succeeded with the same configuration. Existing credentials and valid IP certificate were reused; the original shared Caddy process remained running. The production Mac retained its existing session and DSH runtime when upgraded to the new daemon, and the rebuilt native app was reopened with Remote Control.

Real deployment validation caught two portability defects: macOS tar included AppleDouble files outside the release manifest, and `/tmp` was on a different filesystem from the installation directory. Packaging now excludes that metadata, and installation copies/verifies the release before atomically switching its local symlink. The cross-filesystem failure exercised recovery of the previous service configuration. A later SSH interruption happened before the installer started; server inspection confirmed the previous healthy release, and retry succeeded.

Fresh provisioning on an empty machine, Ubuntu, ARM servers, password-based SSH and arbitrary existing reverse-proxy layouts were not live-tested. The current installer supports private-key/agent SSH and Debian/Ubuntu systemd hosts; existing non-Caddy ownership of ports 80/443 and conflicting IP TLS configuration fail preflight. Network interruption can leave an uncertain server outcome, as documented in `docs/RELAY-INSTALL.md`.


## On-demand history and desktop catch-up (2026-09-10)

Diagnosis of the live host initially found 7,835 events, of which 7,521 were token deltas; the session was idle and all 91 tool calls had a returned result. The desktop loaded the entire raw journal and repeatedly reconstructed the transcript in view reads. The failed `ask_user_question` in the screenshot also had a real error result; its failure was distinct from history/rendering delay.

All clients now use the shared `history.page` projection, initially reading 40 complete records and fetching earlier pages explicitly. Tool input/result pairs stay together; page size also targets 512 KiB. Raw journals are retained. Desktop/PWA compact live deltas and batch presentation updates at 50 ms; native view reads use cached messages and unchanged Markdown views skip repeated parsing. Snapshots cannot be overwritten by older status events. Native reconnect invalidates cached history and fetches the current snapshot; page reads run alongside the live event connection. Complete export still loads all pages explicitly.

Validation passed: the full TypeScript check (63 tests) followed by the sixth history regression (64 total tests across 15 files); 16 Swift tests, including four live checks via `scripts/native-live-check.mjs`. Coverage includes thousands of deltas, original ordering, failed tools, concurrent page/live merges, old-journal migration, bounded byte size, encrypted initial connection without unsolicited full replay, snapshot/listener race, CLI/TUI paging, native complete export and reconnect after an offline completion. `scripts/history-ui-check.mjs` passed encrypted Chrome 390 px checks for a recent 40-record first page, 93 total records after load-more, preserved scroll position, live completion and reload. Its isolated first-page render measured 126 ms on this development machine; that is not a public-network or phone speed guarantee. The existing Markdown browser regression also passed at 320/375/390/1440 px.

The production upgrade waited for idle sessions, then preserved both sessions and the existing DSH process. After upgrade, live first-page RPCs returned 62/63 projected events (40 records including tool pairs), approximately 167/70 KB, in 4/2 ms respectively. The native release was reopened. The public Relay/PWA was updated using the existing one-command deployment profile, reusing credentials and certificates. No model prompts or real shell/file tools were invoked by these checks. Physical iPhone/Safari/cellular behavior remains untested.

The deployed public HTML and JavaScript matched the final local build byte-for-byte; the existing phone returned to confirmed connection state. The native app established its local daemon event connection.


## Linux headless host (2026-09-10)

A Debian 13 x86_64 host was installed through the user's existing SSH access. Node 22.23.2 was verified against the official SHA-256 manifest. DSH and its dependencies are pinned by the portable `config/dsh-runtime` package/lock files (0.1.5-alpha.1); the lock normalizes package paths instead of referring to the development machine. Turnwire production dependencies were installed on Linux rather than copied from macOS.

The new `scripts/install-linux-host.sh` and `install-host-service.mjs` derive all installation paths from their argument, generate a working `turnwire` wrapper and optional user PATH shortcut, and register `turnwire-host.service`. User lingering was enabled. The supervisor starts DSH, captures its short-lived loopback launch URL, and starts the unchanged Turnwire daemon. DSH credentials are loaded from private destination configuration, supplied only to DSH, and redacted from supervisor logs. Target-process inspection verified that DSH has the configured model key, the daemon does not, logs contain neither the model key nor active launch token, and the credentials file is mode 0600.

`npm run check` passes 66 tests in 16 files and all production bundles, including new supervisor readiness, token/key isolation, redaction, graceful shutdown and failed-DSH startup tests. A real SSH PTY verified the remote TUI, runtime status and Relay status. The host uses the existing public Relay under a distinct host identity and a separate phone pairing. Both DSH and the daemon listen only on loopback. Phone/SDK labels now refer to the host and display its reported name, so a Linux host is not described as a Mac.

The Linux user-service restart recovered its original host identity, session and Relay configuration. Fresh SSH login resolves the `turnwire` shortcut; TUI exit leaves the service active and enabled with user lingering. A real public encrypted connection verified that this host is distinct from the existing desktop host, DSH is online, and the same empty workspace is visible remotely. Chrome at 390×844 enabled the composer, received confirmed host presence, and reconnected after reload. One measured encrypted round trip was 455 ms. No model inference prompt was sent. Physical phone/cellular conditions and a full operating-system reboot were not exercised.

## Remote transport v2, inbox, push and LAN (2026-09-10)

- `npm run check`: 75 tests in 20 files pass, with TypeScript checking and production builds. Additional targeted checks after switching presence expiry to a monotonic clock pass; the wall-clock-jump test keeps presence connected until monotonic expiry.
- `node --import tsx scripts/native-live-check.mjs`: 17 Swift tests pass, including the new live inbox, resolved-approval history, direct settings, notification preferences and receipt-query contract. The release app was built, locally signed and reopened at Remote Control.
- Session crypto tests exercise authenticated ECDH, altered transcripts, wrong credentials/device contexts, reflection, replay, out-of-order messages, concurrent encryption ordering, different wall clocks and fresh session keys. Live enrollment checks cover one-use invitations, durable client credential replacement, a lost final enrollment response and rejection of a copied consumed QR.
- A real regression exposed old Relay responses reaching a replacement device connection before its new handshake completed. Relay connection UUIDs now bind payload forwarding and close requests to the correct socket. Rapid reconnect/enrollment integration tests pass after the fix. Existing v1 transport/approval/replay compatibility tests also pass.
- `tests/direct.test.ts` starts a separate TLS server with an explicitly trusted test CA and normal certificate/hostname checking. Direct connectivity, private HTTP-route 404s and automatic Relay recovery are exercised. Product code has no TLS bypass. This loopback fixture is not a real Wi-Fi or iPhone LAN-permission test.
- Push tests use a simulated provider. They verify private SQLite file permissions, persistent VAPID identity/queue, deduplicated delivery, expired-subscription removal, endpoint restrictions, device-scoped subscription authorization and generic notifications. Inbox tests retain resolved/cancelled records and cancel pending runtime approvals across daemon restart.
- `scripts/remote-resilience-check.mjs` passes in actual Chrome at 390×844: one-use pairing, reload, offline/online recovery, simulated visibility events, inbox approval, remembered credentials across tabs, no page errors and no horizontal overflow. The inbox screenshot was inspected after waiting for the drawer transition to complete. Notification delivery/permission is not simulated as a successful OS push in this browser check.
- The existing one-click deployment updated the public Relay and added its private persistent push database. The Mac daemon and Linux host service were updated after verifying idle state; host IDs, sessions and DSH configuration were retained. The Linux model configuration remains 0600. A private application archive supports rollback on the Linux host.
- Ephemeral public v2 pairings verified both real hosts, encrypted snapshots/history, repeated reconnects and Web Push availability without sending model prompts. Observed verification RTTs were 1302 ms for Mac and 1358 ms for Linux in these runs; they are development-machine observations, not speed guarantees. One Linux verification attempt hit the 5-second transport-stage deadline; a subsequent attempt succeeded. No evidence establishes that all WAN retries have been eliminated.
- Chrome loaded the real Linux host via the public HTTPS PWA at 390×844, verified v2, enabled the composer and push-subscription entry, and reconnected after reload with no page errors/overflow. All ephemeral verification pairings were revoked. The real Linux TUI also ran `remote status`, `notifications status`, `remote direct status` and `inbox`, then detached while the service stayed active.
- DSH was upgraded from `0.1.5-alpha.1` (source `5dda764`) to `0.1.5-rc.1` (source `183f08e9`), which is what npm's `latest` tag resolves to. The launcher changed shape: `--patch` is a profile-launcher option now, and the `web` alias refuses parent options, so every launch became `dsh --patch <file> --profile web …` (`package.json`, the host service, and both check scripts). The route also gained a model (`deepseek-flash`, now the default) and the Host gained a sandbox policy: `workspace-write` with `ask`, where commands inside the session workspace or the system temp directory run unasked and anything else raises an approval. On the previous version no real session ever asked for one — 135 bash calls in the live journal, zero approvals — so this is the first release in which the phone's approval controls have real work to do. `scripts/ui-model-check.sh` passed against the new Host (auth, catalog, selection, effort resolution, reload), and the new `scripts/dsh-live-check.mjs` passed a real turn, a real approval, a queued prompt behind it, the runtime's queue projection and the host's dispatch record.
- A finished turn now reads as its result. The steps that produced it — intermediate notes and every tool run — fold into one `Process · N steps` line above the answer, because a reader who comes back to a session wants the answer, not the route taken to it. The live turn stays open step by step (that is where the newest call shows on the collapsed row), and a failed turn keeps its steps visible so a failure is never hidden inside the fold. `scripts/ui-check.mjs` asserts the fold exists exactly once the turn is over, that it is closed with the tool run inside it and the answer visible, and that opening it still shows the flattened, unboxed run.
- A prompt sent while a turn was running used to appear in the flow where it was *written* — in the middle of the answer it was waiting behind — because the journal records it on acceptance and no later event marked its start. Read from the live host: queued prompts were each journaled once, with `queued: true`, and never again. The host now records the moment the runtime starts a waiting prompt (`message.updated` with `queued: false` at the turn boundary, which the live journal shows as an adjacent `idle`/`running` pair), and the shared projection moves that row there, so every client places it in the turn that ran it and stops labelling it as waiting. Covered by a Core test for the event and a projection test for the order, plus a browser assertion that the waiting prompt ends up in the flow without the queued marker.
- Tapping "Choose folder" did nothing on the phone because the host was still serving the build from before the picker existed, and an older host answers a method it does not know with the id `invalid`. A remote client cannot match that reply at all, so it drops it and waits out its own 35-second timeout — a dead button. The host now answers an unparseable request under the requesting client's own id when one can be read (`tests/core.test.ts`), and the picker renders on the tap instead of on the answer, with a reading state and a hint to type the path if the host is older. `scripts/ui-check.mjs` holds the listing back for 1.2s in a service-worker-blocked context and requires the picker within 600ms; the assertion fails on the old rendering and passes on the new one.
- A long waiting prompt used to widen its whole row to the prompt's own width, which made the queue box pannable sideways: measured at 390px, the strip was 358px wide with 1184px of scrollable content and the edit/cancel/steer buttons sitting at x=1188. The cause was the same grid auto-track as the tool groups — a nowrap row contributes its full text width as the track's minimum — so the queue track is pinned with `minmax(0, 1fr)` and the strip never scrolls sideways. The buttons keep their size, the text is cut with an ellipsis, and the full prompt stays on the row's `title`. The running-agent strip had the identical defect with a long child label (384px box, 1154px of content) and the same one-line guard now covers it. `scripts/ui-check.mjs` measures both at desktop and phone widths; it fails on the old CSS with `{"client":714,"scroll":1488}` and passes on the new one.
- A closed tool row now says what the call is doing: the command, path or delegated task read from the call's own arguments, cut with an ellipsis and available in full on the row. A run of calls shows its newest call while it is in flight and its summary once the last one returns, and opening a run indents the calls under a hairline instead of drawing a box inside a box. The offline demo now emits a three-call run whose last call stays in flight for 2.5s, because a client that only ever saw single finished calls never exercised that shape at all; `scripts/ui-check.mjs` asserts the live line, the settled summary, the single open border and real truncation at 390px.
- Choosing a session's working directory no longer means typing an absolute path. `workspace.list` reads one level of the host (folders only, dotfolders skipped, a symlink followed when it leads to a folder, `total` so a truncated level says so), the PWA's new-session dialog walks it, and `turnwire dirs` prints the same list for a terminal. A folder the host cannot read answers `WORKSPACE_UNREADABLE` instead of looking empty. Three Core tests cover the listing, the home default, the filesystem root and the refusal cases; `scripts/ui-check.mjs` walks the picker in Chrome (open, up, home, use-this-folder writes the field) and `scripts/remote-resilience-check.mjs` walks it again at 390px over an encrypted connection, where the action must stay on screen and the list must scroll in its own box. All three browser scenarios and the 138-test suite pass.
- Tab switching was rebuilt as socket reuse rather than reconnection. Hiding the PWA keeps the verified encrypted socket and pauses only the retry timer; a lost network still drops the socket and reports offline at once, and "Reconnect now" still rebuilds the session deliberately. `scripts/remote-resilience-check.mjs` now asserts that hiding and showing the page leaves the health bar `connected` and opens no second WebSocket (it previously asserted the opposite), and `tests/connection-health.test.ts` pins the same contract against a counted peer socket, including a socket that dies while hidden. `npm run check` passed 135 tests in 27 files, followed by that Chrome check and the history Chrome check.

Actual iPhone/Safari permission, lock-screen Web Push delivery, Wi-Fi/cellular transitions, LAN DNS/certificate acceptance and regional push-provider reachability remain unverified. The production LAN listener stays off until the user supplies an appropriate WSS URL and trusted certificate. Current notification `queued` counts the daemon outbox awaiting Relay acceptance; Relay acceptance is not proof of OS notification delivery.
