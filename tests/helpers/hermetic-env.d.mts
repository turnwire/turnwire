/** Explicit fixture overrides only; undefined removes a default for fallback tests. */
export function hermeticEnv(root: string, overrides?: NodeJS.ProcessEnv, source?: NodeJS.ProcessEnv): NodeJS.ProcessEnv;
